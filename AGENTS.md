# 仓库执行规则

## Pre 环境发布

用户要求发布、更新、部署、验证或回滚 Pre 环境时，必须先完整阅读
`deploy/PRE_RELEASE_RUNBOOK.md`，并严格按文档顺序执行。

## 固定流程

1. 从 Runbook 的“固定环境配置”读取构建机、构建代码目录、Git 分支、Pre 机器、Pre 部署目录、镜像仓库和镜像仓库凭据。
2. 登录构建机，在构建代码目录拉取最新 Git 代码，记录准确 commit，构建不可变版本镜像并推送至镜像仓库。
3. 构建机已经预先完成镜像仓库认证，构建和推送阶段不执行 `docker login`。如果推送提示未认证，立即停止并报告。
4. 镜像推送成功后，登录 Pre 机器。标准发布先备份数据；用户明确要求“快速发布”时跳过数据备份。随后登录镜像仓库、拉取刚推送的镜像并运行。

构建机和 Pre 机器可以是同一台机器，也可以是不同机器。无论机器是否相同，构建代码目录与 Pre 部署目录都必须独立配置、分别使用，不得混用。

## 执行约束

- 固定环境配置仍有占位符、为空或无法访问时，必须停止，禁止猜测机器、目录、账号或镜像地址。
- 构建前必须确认目标分支、工作区干净，并记录完整 Git commit。
- 镜像标签必须包含 Git commit SHA，禁止使用 `latest` 发布。
- Pre 部署目录只用于拉取镜像和运行服务，不在该目录执行 Git 拉取或镜像构建。
- 镜像仓库用户名和密码按照当前约定保存在 Runbook。不得在消息、日志、报告或命令输出中展示密码。
- 不得输出 `.env` 中的数据库密码、Django 密钥或 API 密钥。
- 使用 `docker compose config --quiet` 校验配置，避免展开密钥。
- 普通“发布到 Pre”属于标准发布，更新已有环境前必须备份并验证备份。只有用户明确要求“快速发布”时才允许跳过 MySQL、`media` 和 `workspace` 备份。
- 代码拉取、测试、构建、推送、备份、镜像拉取、迁移、健康检查或验收中任一步骤失败时，立即停止，不得继续后续步骤。
- 未经用户明确批准，不得恢复数据库、覆盖持久化数据或删除备份。
- 发布结束后报告 Git commit、镜像地址和 digest、备份位置、迁移结果、容器状态、健康检查、业务验收及回滚情况，但不得包含密码。
